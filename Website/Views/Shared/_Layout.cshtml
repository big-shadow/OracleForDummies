<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>@ViewBag.Title</title>
    <link href="~/favicon.ico" rel="shortcut icon" type="image/x-icon" />
    <meta name="viewport" content="width=device-width" />
    @Styles.Render("~/bundles/c")
</head>
<body class="container">
    <header>
        <h3 id="title" class="u-full-width" style="padding-top:25px">@ViewBag.Title</h3>
    </header>
    <div id="body">
        @RenderSection("search", required: false)
        <section class="content-wrapper main-content clear-fix">
            @RenderBody()
        </section>
    </div>
    <footer>
        <div class="content-wrapper">
            <div class="float-right" style="padding-top:25px">
                <p>&copy; @DateTime.Now.Year - Ray Winkelman</p>
            </div>
        </div>
    </footer>

    <script src="http://code.jquery.com/jquery-1.9.1.min.js"></script>
    @Scripts.Render("~/bundles/j")
    @RenderSection("scripts", required: false)
</body>
</html>
